overwrite: true
schema: "https://gql-customs-dr-production.up.railway.app/graphql"
documents: "gql/**/*.gql"
generates:
  gen/gql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
